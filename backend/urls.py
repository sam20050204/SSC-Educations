from django.urls import path

# NOTE:
# We are intentionally keeping this file minimal.
# All main URLs (login, dashboard, etc.) are handled in core.urls

urlpatterns = [
    # Backend app currently does not expose any URLs
]
